using Domain.Abstractions;
using System;

namespace Domain.Entities
{
    public class DCAppCanonicalURL : Entity
    {
        public DCAppCanonicalURL(Guid id) : base(id)
        {
        }
    }
}